# 환경변수·비밀 (예시 파일만 커밋)
.env
.env.*
!.env.example

.DS_Store
node_modules/

# 로그
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 테스트 커버리지
coverage/
*.lcov
.nyc_output

# Pi 확장 설치 캐시 (node_modules). 매니페스트(.pi/settings.json)만 커밋, 복원은 `pi install`
.pi/npm/

# pi-local-rag 로컬 인덱스 (SQLite, regenerable — 소스 cases/*.md만 커밋)
.pi/rag/

# 로컬 전용 기획/작업 문서 (공개 repo엔 코드 + README만)
plans/
notes.txt

# 워크스페이스 런 산출물(state.json·proposal.md 등)은 기본 무시 — 단 예시 1개만 커밋.
workspace/*
!workspace/w-mqp48cgt/

# 커뮤니티 MCP OAuth 클라이언트 키 (비밀 — 커밋 금지)
gcp-oauth.json

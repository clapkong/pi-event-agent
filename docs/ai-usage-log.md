# AI 사용 로그

> **사람(개발자)이 방향·결정·검증을 주도하고, AI(Claude Code)가 그 지시에 따라 구현을 보조한 기록.**
> 각 행: 날짜 · 도구 · AI가 구현한 것 · **내 역할(지시·결정·피드백·검증)** · 산출물.
> 모든 산출물은 사람이 직접 확인·수정 지시했고, 설계·디자인·우선순위 결정은 사람이 내렸다.

---

## 2026-06-16

| 도구 | AI 구현 | **내 역할 (지시·결정·피드백·검증)** | 산출물 |
|---|---|---|---|
| Claude Code (Opus 4.8) | 정본 plan을 "한 세션=한 태스크" 원자 단위 체크리스트로 분해, V0 검증 스파이크를 선행 단계로 분리 | **분해 방식·V0 선행 분리 방향 지시 및 검토** | `plans/TASKS.md` |
| + impeccable | F0 프런트 스캐폴드(Vite+React+TS·ESM, 라우팅, S0 사이드바, 디자인 토큰·폰트 이식) | **F0 착수 지시(impeccable 사용·토큰은 `DESIGN.md` 따르라 지정), 브라우저 직접 확인, "새 행사 안 보임" 점검 요청→원인 규명 확인** | `frontend/*` |
| Claude Code | F1 연결 계약 + Mock(`contract.ts`·`mockClient.ts`·`harness.ts`) | **다음 단계로 F1 지시, "F1이 뭔지" 설명 요청, 하니스 시퀀스 결과 검토** | `frontend/src/agent/*` |
| + impeccable | F2 S3 동작 타임라인(타임라인·되묻기·승인 게이트·컴포저·모델 토스트) | **F2 지시, "공간을 다 못 쓰는 느낌" 레이아웃 피드백→칼럼폭 수정, 4상태 검토** | `frontend/src/screens/workspace/*` |
| Claude Code | 현재 mock 동작·교체 지점 문서화 | **"지금 mock이고 나중에 실제 데이터로 바꿔야 한다고 `CLAUDE.md`에 적어둬" 지시** | `CLAUDE.md` |
| Claude Code | 앱 셸 풀블리드 전환 + `DESIGN.md` 동기화 | **"카드 형식 말고 메인 화면 전체로" 디자인 결정** | `AppShell.module.css`, `DESIGN.md` |
| + impeccable | F3 기획 폼(S2) + 홈 대시보드(S1): store·타일·알림 센터·달력·체크리스트 | **F3 지시, 확정 시안과의 일치 검토·확인** | `screens/home·new`, `store/*` |
| + impeccable | F0–F3 디자인 점검(이모지→Tabler 아이콘, 홈 버킷 버그, 타이포) + 다음 세션 가이드 | **"impeccable로 점검·수정" 지시, 타임라인 명조 이질감 직접 발견·지적** | `index.html`, `screens/*`, `NEXT_SESSION.txt` |
| (git) | 커밋 정리 | **교수님 레포 확인 지시→마일스톤 단위 커밋 방침 결정, 커밋 키워드 대괄호 제거 결정, 푸시·force-push 판단** | (커밋 히스토리) |

## 2026-06-17

| 도구 | AI 구현 | **내 역할 (지시·결정·피드백·검증)** | 산출물 |
|---|---|---|---|
| Claude Code | 컴포저 텍스트 전달·타임라인 이어붙이기 + 사용자 메시지 표시 버그 수정 | **컴포저·"메시지가 사라지는" 타임라인 초기화 버그 직접 발견·지적** | `S3View.tsx`, `useAgentRun.ts`, `Timeline.tsx` |
| Claude Code | 폼(S2) 입력을 초기 프롬프트로 전달 | **"폼 조건이 자동으로 안 들어간다" 직접 발견·지적** | `NewEvent.tsx` |
| Claude Code | 행사별 워크스페이스 분리(`useAgentRun(wsId)`) + 핸드오프 문서 정리 | **"마무리하고 다음 세션 잘 넘기게 .md 갱신" 지시, 커밋을 의미 단위로 묶기 결정** | `useAgentRun.ts`, `S3View.tsx`, `README.md`·`CLAUDE.md` |
| Claude Code + impeccable | **F4 S4 작업공간 + 상태 모델**: `boardState.ts`(3겹·잠금/신선도·예산 3상태·업체 4단계·stale + `useBoard` 인터랙션), S4 화면(상태 스트립·재기획 배너·조건·예산 막대+표+잠금·업체 미니 스테퍼·지도/날씨·D-레일). 라우트 `/w/:id/board`. 재기획 시나리오(F4.4) puppeteer 3상태 검증. | **"토큰 남은 김에 F4 진행" 결정, 인프라 없는 자율 단위(F4) 선택, "안 더러워지게 한 덩어리만" 방침** | `frontend/src/data/boardState.ts`, `screens/workspace/S4View.*`, `routes/Board.tsx`, 라우팅·TopBar·타일 |

---

## 2026-06-21

| 도구 | AI 구현 | **내 역할 (지시·결정·피드백·검증)** | 산출물 |
|---|---|---|---|
| Claude Code (Opus 4.8) | `.pi/agents/` 6개 서브에이전트 정의를 `@tintinweb/pi-subagents` v0.10.3 정본 사양과 대조·수정: secretary의 잘못된 `mcp` 도구명 제거(로드 실패 버그), researcher·monitor의 `isolated:true` 제거(외부 조사·날씨 접근 차단 버그), writer/monitor/critic 모델을 fuzzy 이름(`haiku`/`sonnet`)으로, Planner 위임 대상을 실제 spawn 타입명(`researcher`/`writer`/`critic`/`monitor`/`secretary`)으로 정렬, monitor의 "깃발 push"→"결과 반환" 멘탈모델 정정 | **"`.pi/agents` 검토·개선" 지시, 범위를 `.pi/agents`로 한정, Planner를 워크스페이스당 1개로 두는 이유 설명→서브에이전트 유지 결정, 모델 fuzzy 방식 채택 결정** | `.pi/agents/*.md` |
| Claude Code (Opus 4.8) | Pi 시스템 프롬프트 빌드 경로 실측(`system-prompt.js`: 기본 "coding assistant" 정체성은 `SYSTEM.md`(=`customPrompt`)가 있으면 통째 교체, 없으면 `AGENTS.md`/`CLAUDE.md`를 `<project_context>`로 덧붙임)으로 "메인=세션 자체" 구조 확정. Planner(`event-organizer-main.md`)를 서브에이전트 디렉터리에서 빼서 **`AGENTS.md`(세션 메인 프롬프트)로 이동**, `.pi/agents/`엔 서브 5개만 남김 | **"행사마다 세션을 따로 관리하니 main을 서브가 아닌 메인으로" 직접 결정, "Pi 내부 너무 건드리는 것 아닌가" 우려 제기→교재(11·12장)로 정식 기능임 확인 후 가장 가벼운 `AGENTS.md` 경로 선택** | `AGENTS.md`(신규), `.pi/agents/event-organizer-main.md`(삭제) |
| Claude Code (Opus 4.8) | 위임 오케스트레이션 명문화: `Agent` 도구 병렬 방식 실측(`tasks[]` 없음 → 한 메시지에 다중 `Agent` 호출 + 각 `run_in_background`, 동시성 4) 확인 후 ① 조사 = researcher **병렬 fan-out**(타입 1개·인스턴스 N), 과거 사례 DB는 main이 `case_search` 직접, ② 작성·검토 = writer ⇄ critic **순차 루프(≤2회)** 를 Planner 프롬프트와 두 아키텍처 문서에 반영. 협업 흐름도도 병렬·루프판으로 교체 | **"researcher 하나보다 여러 개 병렬이 낫지 않나, writer↔critic 관계가 안 드러난다" 직접 지적, 사례 DB 조회 주체=main 직접 선택, 반영 범위=프롬프트+문서 둘 다 선택** | `AGENTS.md`, `docs/AGENTSDOCS.md`, `AGENTS_DETAILS.md` |
| Claude Code (Opus 4.8) | 프런트 구조 진단(pi 패키지 층 관계 실측: `pi-coding-agent` = `pi-agent-core` + 파일도구, `pi-web-ui`도 같은 토대) + 화면 부품 과분할 정리: Home(타일·알림·달력 3파일) → `Home.tsx` 1개, S3 대화(상단바·타임라인·컴포저·토스트·마커 5파일) → `S3View.tsx` 1개로 통합(S4 단일파일 패턴에 맞춤), 설계 참조 주석(§9.x) 제거. 시각 변화 0, `tsc` 통과, 36→28파일 | **"화면만 보여줘야 하는데 로직·파일이 너무 많다" 직접 진단→로직은 `boardState`·`useAgentRun` 2곳에 모여있음 확인, "부품을 너무 쪼개놨다, S4처럼 한 파일로" 통합 방향 결정, 파일 수↓ vs 파일 길이↓ 트레이드오프 중 전자 선택** | `screens/home/Home.tsx`, `screens/workspace/S3View.tsx`(통합), 8파일 삭제 |

| Claude Code (Opus 4.8) | 에이전트 `.md` 프롬프트 엔지니어링 패스(한 파일씩): 5개 서브 + `AGENTS.md`에 **입력 계약·출력 계약·역할 경계** 신설. researcher=다후보(검증된 것만·추천순)·web_search/fetch_content, writer=블록 포맷(`[잠금]`·`[n]`)+inherit_context, critic=판정 키워드+치명/권고+**수정 방향·처리 주체 분류**+inherit_context+tools=read, secretary=**추출만**(분리형), monitor=`판정: 재기획` 구조화. `AGENTS.md`에 조사 입력→**선택 게이트(ask_user_question)**→writer↔critic **루프 라우팅**→**변화 대응** 전 구간 배선 + ask_user_question 사용 정책·예시. 미구현 도구(`case_search`·`update_state`) TODO 표시. 두 아키텍처 문서 흐름도 동기화. 루트 `TODO.md` 신설 | **방식=한 파일씩 같이, 목표=역할별 꼭 필요한 것+입출력 구조화 직접 지정; researcher 다후보·secretary 분리형·case_search=main 직접·inherit_context(writer/critic)·critic이 수정방향+처리주체로 무한루프 차단 등 설계 결정; "ask_user_question 호출 가능?"·"호출할 줄 모르지 않나" 직접 점검→사용 정책 명문화 유도; TODO.md 생성 지시** | `.pi/agents/*.md`, `AGENTS.md`, `AGENTS_DETAILS.md`, `docs/AGENTS_DETAILS.md`, `TODO.md` |

| Claude Code (Opus 4.8) | 헤드리스(`pi -p`)로 secretary·monitor 실측 검증 → **secretary가 vague spawn 시 회의록을 환각**하는 것 발견·수정(secretary 최상위 환각 가드 "원본 못 읽으면 지어내지 말고 보고" + `AGENTS.md` "자기완결 지시 원칙": inherit_context 꺼진 서브엔 입력 출처 명시). monitor는 `판정: 재기획 제안` + 잠금/집행 제외 정상 확인. 이어 **monitor·secretary 트리거 소유권을 Planner→시스템(백엔드)으로 reframe**(운영 기간엔 활성 대화 없음 → 백엔드가 트리거, 결과만 세션 유입, Planner는 대응): `AGENTS.md` 4곳 + 두 아키텍처 문서 + `TODO.md`에 트리거 인프라 항목 | **"전체 파이프라인 시험" 지시 + 더미 워크스페이스 설정 요청, secretary 환각 직접 포착, 다른 세션의 트리거-소유권 분석을 가져와 "monitor·secretary는 시스템 트리거여야 한다" 결정** | `.pi/agents/secretary.md`, `AGENTS.md`, `AGENTS_DETAILS.md`, `docs/AGENTS_DETAILS.md`, `TODO.md`, `plans/test/*`(로컬) |

| Claude Code (Opus 4.8) | **데이터 계층 pgvector→파일 전환 + 사례 RAG 익스텐션 채택**: `@earendil-works` 스코프엔 RAG 패키지 없음 확인 후 써드파티 Pi RAG 익스텐션 탐색(`@artale/pi-rag` 키워드 vs `pi-local-rag` 하이브리드). `pi-local-rag` 실제 설치(`pi install -l`)·현재 Pi 0.79.8 로드 검증(bogus 키 런이 모델호출 401까지 도달=로드 에러 없음, 옛 `@mariozechner` import도 `pi-web-access`처럼 정상 해결). 명세(`DETAILS.md`) DB 미요구 확인. 문서 9개의 PostgreSQL/pgvector/`case_search`/`case_insert`/사례 DB → `.md 파일 + pi-local-rag(rag_query/rag_index)`로 일괄 갱신, 사례 `citedBy` 순환은 frontmatter 파일 edit로 유지 명시 | **"pgvector는 어디서 나온 거냐" 출처 추궁→AI 기획 문서가 끼워넣은 것 규명, "DB 유지 이유 있나·.md 원해" 직접 결정, "의미 검색 필요하지 않냐" 판단해 키워드(@artale) 대신 pi-local-rag 선택, "-l 프로젝트 로컬" 지정, "네가 실험해봐"로 로드 검증 지시** | `plans/{ARCHITECTURE,DATA_MODEL,PLAN,TASKS,PI_ELEMENTS,SCREENS,BUILD}.md`, `README.md`, `docs/DESIGN.md`, `.pi/settings.json` |

| Claude Code (Opus 4.8) | **MCP 외부 도구 연동 — 하이브리드 Gmail**: `pi-mcp-adapter`로 ① 검색=`pi-web-access`(무설정, `web_search` 헤드리스 검증) ② **공식 Gmail 원격 MCP**(`gmailmcp.googleapis.com`, OAuth) 읽기·검색·초안 ③ **자작 발송 MCP 서버**(`mcp-servers/gmail-send`: `@modelcontextprotocol/sdk`+`googleapis`, `send_email`→**elicitation 승인 게이트**→`messages.send`, refresh token=`.token.json`) 신규 작성·연결. 연결 실패 연쇄를 단계별 격리·해결(어댑터가 `oauth.*` 필드 `${}` 미보간→`.pi/mcp.json` 리터럴, 테스트 사용자 미등록 `access_denied`, 포트 3118 점유 CSRF, 일반 `gmail.googleapis.com` 미사용). 직접 발송 진단(본인 1통)으로 토큰·스코프·API·발송코드 검증. 재현·트러블슈팅 가이드 작성 | **MCP 연결 지시, third-party Gmail 서버 거부(공식/자작만)·실발송 필수 요구→하이브리드 결정, "MCP 형식 유지" 요구로 자작 MCP 서버 채택, 크레딧 절약(헤드리스 최소화) 지시, 각 에러 화면 직접 제보, 교수님 재현 가능성 질문, 가이드 문서 요청, 직접 발송 진단 승인** | `mcp-servers/gmail-send/*`, `.mcp.json`·`.pi/mcp.json`(로컬), `.gitignore`, `docs/MCP_GUIDE.md` |

| Claude Code (Opus 4.8) | **PI_ELEMENTS 카탈로그 재분류 + Skill 4종 작성**: skill/tool/extension 정의에 맞춰 v1 대폭 정리 — **Agent·Hook 카테고리 신설**, `custom tool ≠ Extension`(createExtension 묶음=과제 요건) 정정, **tool 13→3**(estimate_budget·build_checklist·update_state)·**skill 13→4**·**플러그인 8→3**으로 cut하고 버린 것은 "흡수 맵"으로 누락 0 추적(vendor/dietary/accessibility→agent 프롬프트, send_email→자작 Gmail MCP, case→pi-local-rag, 5종→risk-assessment omnibus 통합, PDF export→pi-markdown-preview). 정본 교체(v2→PI_ELEMENTS.md, v1 폐기). `.pi/skills/`에 `budget-policy`·`notice-writer`·`risk-assessment`·`satisfaction-survey` 작성, 잔재 루트 `skills/` 제거, TODO 갱신 | **"skills vs custom extension 차이" 학습 요청→현재 agent 기준 재분류 지시, "너무 보수적·쓰레기 계획서, 필요한 것만 취하고 버려라" 공격적 cut 결정, 항목별 직접 결정(vendor/dietary/accessibility=프롬프트 흡수·send_email=커스텀 MCP·case=pi-local-rag·5종 누락금지·markdown preview 채택+PDF 제외), skill↔agent 흡수 기준 질문, 비교용 별도파일(v2) 요구, A(Skill 작성) 선택·작업 위치 확인** | `plans/PI_ELEMENTS.md`, `.pi/skills/*/SKILL.md`(4종), `TODO.md` |

| Claude Code (Opus 4.8) | A 파이프라인 실측에서 버그 2건 진단·수정 — ① **fuzzy `haiku`가 구형 `claude-3-haiku`(도구 미지원·출력 4.1K)에 오매칭**돼 writer 빈출력 → writer/critic/monitor 모델을 명시 ID(`anthropic/claude-haiku-4.5`·`anthropic/claude-sonnet-4.6`)로 핀. 헤드리스 재테스트로 writer 정상 출력 + **writer⇄critic 루프 끝까지 검증**(판정 키워드·처리주체 분류[writer/재조사/사용자 판단]·writer 재작성 1회·통과; critic이 케이터링/장소 예산 비현실성·추석 충돌까지 포착). ② `ask_user_question` **5옵션 검증 실패** → "질문당 2~4개, 초과 시 추천순 상위 4+Other" 규칙 + writer/critic **foreground 회수 힌트** 추가. (앞서 secretary·monitor도 헤드리스 검증, secretary 환각 가드 추가) | **"전체 파이프라인 시험"·"테스트 해보자" 지시, pi 실행 로그 직접 제공·관찰(선택게이트·병렬 관전포인트 지정), 버그 현상 보고** | `.pi/agents/{writer,critic,monitor,secretary}.md`, `AGENTS.md`, `plans/test/*`(로컬) |

## 2026-06-22

| 도구 | AI 구현 | **내 역할 (지시·결정·피드백·검증)** | 산출물 |
|---|---|---|---|
| Claude Code (Opus 4.8) | **백엔드 B(pi rpc↔WS 브리지) 구축·브라우저 실증**: 방식 A(SDK 인프로세스) vs B(`pi --mode rpc` 서브프로세스)를 공식문서·교수님 레퍼런스·rpc.md로 비교 후 **B 확정**. `backend/`(TS+tsx) 책임분리 구축 — config·workspace·rpc/contract 타입 + **bridge**(순수 변환, 단위테스트 10)·**pi-session**(JSONL LF split)·**ws-handler**·**server**(Fastify /ws·/health). 프런트 실연결: contract/realClient/mockClient에 `close()` + `useAgentRun`를 effect-lifecycle로(StrictMode 이중연결 수정)·main.tsx StrictMode 임시 off. **브라우저 풀 파이프라인 실증**: 연결 유지 시 백그라운드 서브 완료→followUp 자동 이어짐→`get_subagent_result` 회수→종합→`ask_user_question`까지. 진단: rpiv-ask-user-question이 `ui.custom`(TUI 전용)이라 웹 비호환→자작 ask 필요 규명, setStatus 등 표시용 ui 이벤트 무시 매핑, cwd=레포루트 임시(공유 agentDir는 TODO) | **A/B 차이를 끝까지 캐물어 직접 판단(터미널과 "똑같이" 동작 요구), "구조부터—한 파일에 다 넣지 말라" 지시, contract 정본=백엔드 결정(사본 거부), 브라우저 직접 실행·화면/로그 제보, 더블연결·완료 조기표시 현상 보고, 잡파일 정리·우리 파일만 커밋 지시** | `backend/*`, `frontend/src/agent/{contract,realClient,mockClient,useAgentRun}.ts`, `frontend/src/main.tsx`, `TODO.md` |

| Claude Code (Opus 4.8) | **`event-tools` Pi Extension 구현** (과제 'Pi Extension' 요건): 결정론적 도구 3(`estimate_budget` 배분/재배분·`build_checklist` D-30/7/1·`update_state` 보드 쓰기 유일통로) + 가드 훅 3(잠금🔒·집행·승인게이트). 설치 pi 0.79.8 소스 + **공식문서(pi.dev)** 대조로 API 확정(`export default (pi)` 팩토리·`pi.registerTool`·`pi.on('tool_call')→{block}`·`ctx.ui.select`). **공식 준수 교정**: `Type`←`typebox`, enum←**`StringEnum`**(Google/Gemini 호환, `Type.Union/Literal`은 비호환). 상태 계약 `<cwd>/state.json`=`boardState.ts` 미러(backend는 읽기만). 가드 단위테스트 8/8 + 3도구 헤드리스 동작 검증(`build_checklist` UTC→로컬 타임존 버그 수정). 교수님 `work-tools.ts` 컨벤션 맞춰 **단일 파일** `.pi/extensions/event-tools.ts`(중간에 split했다 번잡해서 통합) | **"event-tools 빌드" 지시, 교수님 예시 리포(`pi-work-agent`)와 직접 비교 요청→파일명/위치 정렬 지시, "테스트가 모델 없이 되나"·"공식이랑 맞아?" 검증 추궁→StringEnum·typebox 교정 유도, "test.ts가 번잡" 지적→단일 파일 통합 결정, 정리 커밋 내리고 본 커밋에 합치라 지시** | `.pi/extensions/event-tools.ts`, `AGENTS.md` |

| Claude Code (Opus 4.8) | **Skill 4종 자산화(.md만→ assets/references 확장) + extension 불일치 버그 수정**: skill 구조가 SKILL.md 하나뿐이라 빈약 → 이 아키텍처에선 `scripts/`는 extension 몫이라 제외하고 **`assets/`(재사용 템플릿)·`references/`(on-demand 심화)만 보강**으로 방향 잡음. ① **budget-policy**: 유형별 배분표를 `assets/ratios.json`(단일 소스)로 빼고 **`estimate_budget`이 그 파일을 읽도록** 수정 — 기존 코드의 `DEFAULT_RATIOS`(5항목 단일비율, skill 표와 항목·비율 모두 불일치) 버그 해소. `fileURLToPath`로 경로 해석 + 폴백 + `keywords` 유형 매핑(노드로 합1.0·매핑·배분합 검증). ② **notice-writer**: 메일 템플릿 6종(초청·리마인더·취소변경·감사 + **업체 RFQ·발주확인**, vendor 단계 매핑). ③ **satisfaction-survey**: `survey-template.json`(권장 문항+분석규칙). ④ **risk-assessment**: `references/permits-insurance.md`(인허가·보험·소방 심화)+`checklist-template.json`. 각 SKILL.md에 자산 포인터 연결 | **"extension·skill 둘 다 봐라, 내 skill은 다 .md라 빈약하다" 문제 제기, 4종 전부 보강 + notice-writer에 업체 컨택 메일 추가 결정** | `.pi/skills/*/assets/*`, `.pi/skills/risk-assessment/references/*`, `.pi/skills/*/SKILL.md`(4종), `.pi/extensions/event-tools.ts` |

| Claude Code (Opus 4.8) | **MCP — 공식 게이팅 규명 후 커뮤니티+전역 채택**: 공식 Google Workspace 원격 MCP(`gmailmcp`/`calendarmcp`)가 `The caller does not have permission`(프로젝트 로그 미생성=서비스 경계 차단)으로 막힘을 **gcloud로 전 변수 직접 검증**(API enable·프로젝트 owner·토큰·스코프·resource·`x-goog-user-project`·신원스코프 전부 정상, **직접 Google API는 작동**)해 **Developer Preview Program 등록 게이팅**(승인 며칠·조직 단위, 학생 불가)으로 확정. 개인·yonsei 둘 다 불가(yonsei는 카드 결제도 막힘). → **커뮤니티 npx MCP 3종 채택**: gmail `@gongrzhe/server-gmail-autoauth-mcp`·calendar `@cocal/google-calendar-mcp`·maps `@modelcontextprotocol/server-google-maps`, **개인 계정+새 Cloud 프로젝트**, 표준 API라 게이팅 없음·refresh 자동갱신. gcloud로 API enable·Maps 키 생성. **전역 `~/.config/mcp/mcp.json`**(모든 워크스페이스 공유)으로 이전, 옛 자작·공식 잔재(`mcp-servers/`·`setup.mjs`·`.env.example`·`MCP_GUIDE.md`) 제거. 브라우저 실증: gmail 라벨·calendar 일정·maps 카페검색 전부 MCP 경로 작동. 상세문서 신규 | **공식 안 풀리는 것 끝까지 의심·검증 요구("제대로 좀")·각 오류/로그 직접 제보, gcloud 설치로 진단 위임, 자작 거부→커뮤니티 선택, 카드 막힘→개인계정 전환 제안, 전역화·잔재 정리·상세문서 요청** | `~/.config/mcp/mcp.json`(전역), `README.md`, `docs/MCP_DETAILS.md` (제거: `mcp-servers/`·`docs/MCP_GUIDE.md`·`.env.example`) |

| Claude Code (Opus 4.8) | **확장·스킬 2차 점검 — 죽은 파이프라인 2건 + 데이터손실 footgun 수정, 예비비 정책 인하**: 비판적 재검토로 3건 발견·수정. ① **build_checklist→보드 연결 복원**: `build_checklist`가 milestones를 계산해도 `update_state` 파라미터에 `milestones`가 없어 **D-레일에 영영 안 들어가던 단절** + dday 부호 규약이 프런트(음수=행사전)와 **반대**임을 실측 → build_checklist가 보드형 milestones(부호 반전·status 계산)를 `details`로 내주고 update_state에 `milestones`·`replanChangedInputs`(재기획 배너, Gemini 비호환 `Type.Union` 회피해 배열로) 쓰기 추가. ② **배열 데이터손실 방지**: `update_state`가 `{...cur,...patch}`라 budget/vendors/conditions를 일부만 보내면 **나머지가 통째 삭제**되던 footgun → id(conditions=key) 기준 `mergeById` 병합으로 교체(가드는 그대로). ③ **예비비 10%→5% 인하**(`ratios.json`+`reserveFloor`+SKILL 표·하한 문구 동기, 낮춘 5%p는 나머지 항목 비례분배해 합 1.0 유지). **검증**: jiti(=Pi 실제 로더)로 수정 확장 로드+4도구 실행 — milestones 6개 기록·부분 budget 갱신 시 타항목 보존·replan 설정·합1.0 전부 통과 | **"또 고칠 부분 없어?" 점검 지시, 발견 3건 중 1·3 수정 채택(2는 하한강제 대신)·"예비비 너무 많아" → 5% 인하 결정(승인 프리뷰 기준)** | `.pi/extensions/event-tools.ts`, `.pi/skills/budget-policy/{SKILL.md,assets/ratios.json}` |

| Claude Code (Opus 4.8) | **도메인 확장 — 학생회 행사 6종 추가(코드 변경 0)**: 기업 전용→학생회 겸용. 엔진이 이미 도메인 중립(프롬프트에 기업 가정 거의 없음, `estimate_budget`이 `byType[유형]` 키를 그대로 써서 유형별 항목명 자유)임을 확인하고 **스킬 데이터/문구만** 확장. ① `ratios.json`에 새터·MT·축제/주점·간식행사·정기총회·체육대회 6유형(캠퍼스 장소 저렴→교통·주류·안전 비중↑, 유형별 다른 항목명) + 라우팅 키워드, `"축제"` 키워드를 기업 페스티벌→학생 축제로 재배치. ② budget-policy 표에 학생회 섹션. ③ notice-writer 학생 템플릿 2종(학우 친근체 공지·학교 행정 격식). ④ risk-assessment `references/student-events.md`(미성년 음주·부스 화기·MT 이동/야간·캠퍼스 승인=기본 '치명'). ⑤ survey areas 교체 메모. **검증**: 10유형 합 1.0 + 학생/기업 라우팅 + jiti로 실제 확장 로드해 학생 유형 estimate_budget 정상 출력 | **"학생회로 확장하면 일 많냐" 질문→"코드 거의 안 건드린다" 답 확인 후 "적당히 해" + 유형 6종 지정** | `.pi/skills/budget-policy/{SKILL.md,assets/ratios.json}`, `.pi/skills/notice-writer/{SKILL.md,assets/notice-student.md,assets/email-campus-admin.md}`, `.pi/skills/risk-assessment/{SKILL.md,references/student-events.md}`, `.pi/skills/satisfaction-survey/SKILL.md` |

| Claude Code (Opus 4.8) | **보드 상태 동기화 (REST GET, update_state→프런트 보드)**: 방금 고친 `update_state`가 쓰는 `workspace/<id>/state.json`을 프런트 보드가 읽게 연결. 백엔드 `server.ts`에 `GET /api/workspaces/:id`(state.json JSON passthrough, 없으면 404) + CORS 헤더(단순 GET이라 preflight 없음), `workspace.ts`에 `stateFile()` 헬퍼(읽기용·폴더 생성 안 함). 프런트 `boardState.ts`: `fetchBoard()`(emptyBoard 병합으로 데모값 혼입 방지) + `useBoard`가 빈 보드로 시작해 실제 state.json 으로 채움 — **mock seed 폴백 제거**(보드는 백엔드 데이터만 반영, 없으면 빈 채). **검증**: 백엔드 기동해 404·실제 state.json 서빙·CORS 확인, 브라우저 e2e(state.json 주입→새로고침→보드 전 필드 교체), 백/프 tsc 클린 | **TODO에서 ②(보드 REST) 선택, 검증 후 "프런트 폴백 없애줘"로 seed 제거 지시** | `backend/src/{server.ts,workspace.ts}`, `frontend/src/data/boardState.ts`, `TODO.md` |

| Claude Code (Opus 4.8) | **데이터 mock 교체 ② — 워크스페이스 목록/생성 REST**: 보드 동기화에 이어 사이드바·홈 타일의 워크스페이스 목록을 백엔드로. 각 mock의 실제 출처가 달라(보드=state.json·목록=meta·제안서=writer산출물·알림=미정·사례=RAG) 단계적 접근으로 정리하고 토대인 목록/생성부터. backend: `WorkspaceMeta`(타일용, 보드 state.json 과 별개) + `listWorkspaces()`(meta.json 있는 폴더만, 최신순)·`createWorkspace()`, `GET/POST /api/workspaces`(name 없으면 400). frontend `store/workspaces.tsx`: 마운트 시 GET 로드, `add`는 프런트 id 생성→낙관적 prepend+POST 영속(컴포넌트 무수정), **SEED_WORKSPACES mock 제거**(빈 목록서 시작). **검증**: 백엔드 기동해 GET 빈목록→POST 생성·meta.json 기록→400→GET 반영, 백/프 tsc 클린 | **"데이터 mock 교체 이어가자" 지시 — 출처별 분해 후 토대부터 진행** | `backend/src/{server.ts,workspace.ts}`, `frontend/src/store/workspaces.tsx`, `frontend/src/data/workspaces.ts`, `TODO.md` |

| Claude Code (Opus 4.8) | **날씨 백엔드 모듈 `get_weather` (Open-Meteo, 키 불필요)**: 외부 날씨를 **백엔드 모듈**로 결정(표시-우선·`state.json` 저장·프런트 REST 읽기, weather는 잠금 없는 필드라 update_state 단일-writer와 무관). `backend/src/weather.ts getWeather()` — 행사일까지 **≤16일=실제 예보 / 그 이상·과거=과거 N년 같은 월-일 평년값**(평균+변동폭, 강수일 비율=pop), `source`(forecast\|climatology)·`basis`(어떤 날 기준인지 한 줄: 예보일·D-N / 평년 연도범위·기준일)로 구분. 출력 `{label,temp,pop,stale}`=state.json weather 호환(+source/basis). 지오코딩은 Open-Meteo가 영문 도시만 → 한글·랜드마크는 lat/lon 직접(maps MCP 좌표). `server.ts`에 `GET /api/workspaces/:id/weather`(fetch→state.json.weather 병합 저장→반환). **검증**: tsx 스모크로 예보(D+3)·평년(D+120)·영문 지오코딩·한글 에러 + 엔드포인트 e2e(curl→state.json 기록→보드 GET 서빙) 확인, tsc 클린. (검증용 임시 스모크 스크립트는 발표용 정리로 제거 — 실테스트는 `bridge.test.ts`) | **"이건 backend tool 아니냐(표시-우선·저장·읽기)" 직접 지적→백엔드 모듈로 결정, 스펙(예보/평년 분기·기준일 표기·state.json 호환) 지정** | `backend/src/{weather.ts,server.ts}`, `TODO.md` |

| Claude Code (Opus 4.8) | **Planner→보드 쓰기 배선 + 빈 보드 NaN 가드**: 실제 기획 실행 후 보드가 텅 비고 `집행 NaN%`로 뜬 것 진단 → 원인 둘. ① `workspace/`에 state.json 부재 = Planner가 채팅만 하고 `update_state`를 안 부름(AGENTS.md에 도구 존재만 적혀있고 "기획 중 보드에 적어라" 단계가 없었음). **AGENTS.md에 "보드 반영(update_state)" 단계 신설** — 조건 확정→conditions/stage/budgetTotal, estimate_budget 직후→budget, build_checklist 직후→`details.milestones`, 장소·업체·날씨→venue/vendors/weather를 기획 진행 중 그때그때 반영(배열 id 병합, 가드 차단 시 보고). ② seed 제거로 빈 보드(budgetTotal 0)가 되며 `spent/total` 0-나눗셈 → **S4View 2곳 가드**(spentPct·BudgetBar). 검증: frontend tsc 클린. 라이브 동작은 다음 실행 때 확인(보드 자동 재조회는 미구현 → 실행 후 새로고침 필요) | **실제 기획 실행 화면(빈 보드·NaN) 제보 → "보드에 안 적힌다" 원인 추적 위임, "해줘 너가"로 배선 지시** | `AGENTS.md`, `frontend/src/screens/workspace/S4View.tsx` |

| Claude Code (Opus 4.8) | **캘린더 MCP를 공휴일·요일 판단에 활용 (날짜 결정·평가)**: Planner(`AGENTS.md`)에 날짜·마감 결정 시 캘린더 MCP 사용 지침 — `calendar_get-current-time`(기준일), `calendar_list-events`로 **대한민국 공휴일 캘린더**(`en-gb.south_korea#holiday@…`) 조회해 공휴일·연휴 파악 + 행사일/마감 **요일**(평일·금·토·일) 반영, 결과를 writer·critic 맥락에 사실로 포함. critic(`tools:read`라 직접 호출 불가 → inherit_context로 부모 맥락 활용)엔 리스크 점검에 **행사일 공휴일·연휴·주말 / 마감 주말·공휴일 충돌** 추가(맥락에 정보 없으면 "캘린더 확인 필요" 플래그·미통과). 이어 **지도 MCP 활용**(`maps_maps_geocode`·`maps_maps_search_places`=좌표·주변편의·평점, 거리·경로 도구=접근성·이동시간) + **"연결된 MCP 적극 활용·없는 값 비조작"** 원칙(Gmail=업체 회신/견적 확인, 발송은 승인 게이트)을 AGENTS.md에 추가. | **"calendar MCP를 fully 활용 — 날짜·critic 평가 때 공휴일·금토 고려" + "지도 등 다른 MCP도 고려하게, AGENTS.md에 추가" 요청** | `AGENTS.md`, `.pi/agents/critic.md` |

| Claude Code (Opus 4.8) | **타임라인 영속(재방문 시 대화 복원)**: 워크스페이스를 떠났다 오면 대화가 사라지던 문제 진단 — 백엔드 pi 세션은 `--session-id`로 resume돼 **에이전트 기억은 유지**되지만 프런트 타임라인이 라이브 이벤트로만 그려져 재마운트 시 빈 화면(S3View는 `key={ws.id}`로 remount). 자체완결 화면 캐시로 해결: `useAgentRun`이 `RunState`(직렬화 가능)를 **localStorage `agentRun:<wsId>`에 저장**, 마운트 시 lazy init로 복원(스트리밍 커서 끔·running/pending 리셋으로 멈춘 스피너 방지·idRef 시드로 키 충돌 방지). 백엔드 없어도 봤던 대화 복원. 검증: frontend tsc 클린. 후속(더 정확): 백엔드가 세션 transcript를 replay | **"대화 내용이 있었는데 다시 오면 사라지고 작업공간도 빔" 제보** | `frontend/src/agent/useAgentRun.ts` |

| Claude Code (Opus 4.8) | **보드 state.json 경로 불일치 해결 — 세션 ID 키 라우팅**: 기획은 보드를 채웠는데(AGENTS.md 배선 작동) 화면이 빈 원인 = `update_state`가 **레포 루트 `state.json`**에 쓰고(ws-handler가 pi를 cwd=REPO_ROOT에서 띄움 — pi가 cwd/.pi만 보고 walk-up 안 해서 뇌 때문에 불가피) 백엔드는 `workspace/<id>/state.json`을 읽어 어긋남. 해결책 후보(cwd=행사폴더+심링크 / env로 경로 주입)는 "스파게티"라 기각 → **사용자 제안 채택: 채팅 세션 ID로 분리.** pi 가 `--session-id ws-<wsId>`로 떠 있으니 확장이 `process.argv`에서 그 ID를 읽어 wsId 도출, `<cwd>/workspace/<wsId>/state.json`(백엔드가 이미 읽는 경로)에 직접 씀. **cwd·심링크·env 안 건드림, 백엔드 변경 0.** writeState 가 폴더 보장(mkdir). 검증: jiti로 `--session-id` 시뮬레이션→`workspace/<wsId>/state.json`에 쓰고 루트엔 안 씀 확인, 백엔드 tsc 클린, 루트 찌꺼기 state.json 제거 | **심링크·env 방식 연속 기각("너무 스파게티 싫어") 후 "세션에 json 만들고 workspace 정보+채팅 세션 ID를 키로" 직접 제안 → 채택** | `.pi/extensions/event-tools.ts`, `backend/src/{ws-handler.ts,pi-session.ts}` |

| Claude Code (Opus 4.8) | **프롬프트 재주입 버그 + 장소 지도 + 실제 캘린더**: 라이브 사용 중 제보 3건. ① **autostart 재주입**: 작업공간↔AI 왕복 시 `location.state.autostart`가 남아 S3View remount마다 프롬프트 재발사 → `state.entries.length===0 && !running`일 때만 발동(타임라인 영속과 맞물려 재방문/새로고침에도 대화 유지). ② **장소 지도**: S4 venue 카드에 Google Maps 임베드(`?q=이름&output=embed`, 키 불필요) — venue.name 있을 때. ③ **홈 캘린더 실데이터**: 하드코딩 6월/mock 마크 → 현재 월 + **한국 공휴일**(Nager.Date 공개 API, 키 불필요·CORS `*` 확인, 2026 KR 18건) + **행사 일정**(워크스페이스 보드 milestones due) 오버레이. CSS `.dayHoliday` 추가. 검증: frontend tsc 클린 + Nager KR/CORS 확인. 남은 mock=홈 알림·체크리스트(알림 소스 미정에 막힘) | **라이브 제보: "작업공간 mock 남음·지도/구글캘린더 보고 싶음·AI 재진입 시 프롬프트 재주입" → 캘린더는 '공휴일+행사 오버레이', 지도+캘린더 둘 다 이번에 선택** | `frontend/src/screens/workspace/S3View.tsx`·`S4View.tsx`, `frontend/src/screens/home/Home.tsx`·`home.module.css` |

| Claude Code (Opus 4.8) | **meta.json↔state.json 통합 (워크스페이스 파일 1개)**: 새로고침 후 워크스페이스 목록이 빔 진단 — 목록은 `meta.json`(폼 생성·이름 보유)만 읽는데 해당 폴더엔 `state.json`(보드)만 있어 누락. 두 파일·두 writer가 어긋나는 스파게티 → **행사당 파일 하나(`state.json`)로 통합**: 이름을 state.json 에 저장하고 타일(status·summary·progress)은 **보드에서 파생**(stage→status, 마감 지난 미완료 마일스톤→late, 조건 유형·인원+예산→summary, 마일스톤 완료율→progress). `meta.json` 폐기. backend `workspace.ts`만 수정(listWorkspaces=state.json 스캔+deriveTile, createWorkspace=초기 state.json 기록) — **프런트·확장 무수정**(이름이 JSON에 같이 실려 update_state 병합서 보존). 검증: 생성→name 저장→update_state 후 name 보존→목록 파생 + 기존 orphan(state만 있던 폴더) 복구 확인, tsc 클린 | **"state.json과 meta.json 뭐가 다르냐" 질문 → 차이 설명 후 "합치고 코드도 통일" 결정** | `backend/src/workspace.ts` |

| Claude Code (Opus 4.8) | **타임라인에 서브에이전트 표시(critic 도는지 보이게)**: 기획 중 어떤 서브에이전트가 도는지 화면에서 안 보이던 문제 — Planner가 `Agent` 도구로 스폰하면 타임라인 라벨이 범용 "Agent"라 critic/writer/researcher 구분 불가. Pi 소스 실측: `tool_execution_start` 세션 이벤트가 `args`(도구 입력) 포함, rpc-mode가 원본 이벤트를 stdout으로 그대로 forward → 백엔드가 받음. `@tintinweb/pi-subagents` 확장에서 스폰 도구명=`Agent`·인자키=`subagent_type` 확인. bridge: Agent 스폰이면 `args.subagent_type`으로 라벨링(critic→"critic · 검토", writer→"초안 작성", researcher→"조사" 등), rpc.ts 타입에 `args` 추가. foreground인 writer/critic은 실행 동안 active 엔트리로 보임. args 없으면 "Agent"로 폴백(무회귀). 검증: backend tsc + 테스트 11개(신규 1) 통과 | **"critic이 돌아가는지 모르겠다" 가시성 제보** | `backend/src/{bridge.ts,rpc.ts,bridge.test.ts}` |

| Claude Code (Opus 4.8) | **워크스페이스 셸 — 패널 전환에도 에이전트 안 죽게(연결 지속)**: 실런에서 작업공간↔AI 이동 시 런이 끊기던 근본원인 진단 — WS 연결이 S3View 컴포넌트에 묶여 언마운트 시 `client.close()`→백엔드 `socket close`→`session.close()`로 **pi 프로세스 kill**(라우트가 평면 형제라 패널 전환=언마운트). 해결: `/w/:id`를 **중첩 레이아웃(`WorkspaceShell`)**으로 바꿔 연결을 셸이 소유 + `AgentRunContext`로 패널에 제공. 패널(AI 에이전트·작업공간·문서)을 **탭+Outlet 스왑**으로 전환 → 셸은 안 언마운트 → 연결·런·타임라인 유지. autostart(프롬프트 1회 주입)도 셸로 이동(대화 있으면 무시). 행사 전환 시 `key={id}`로 셸 remount(새 연결). S3View는 `useAgentRunCtx()` 소비로 전환(자체 useAgentRun·autostart 제거). 검증: frontend tsc 클린. 남은 후속: #2 진행표시·서브에이전트 내부출력 스트리밍, #4 보드 일정 옆 미니달력 | **실런 관찰 후 "작업공간 가도 기존 탭 계속 유지돼야(패널 바꾸는 것처럼)" 요구 → 셸 구조 컨펌** | `frontend/src/agent/AgentRunContext.tsx`(신규), `frontend/src/routes/{WorkspaceShell.tsx(신규),Workspace.tsx,Page.module.css}`, `frontend/src/App.tsx`, `frontend/src/screens/workspace/S3View.tsx` |

| Claude Code (Opus 4.8) | **진행 표시(경과 시간) + 보드 일정 미니달력**: ① 긴 단계(critic 6분 등)에 "도는 중"이 안 보이던 문제 → 타임라인 active 도구 항목에 **라이브 경과 시간 + 맥동 점**(ToolEntry.startedAt 추가, `Elapsed` 컴포넌트가 active 동안 1초마다 갱신, mm:ss "진행 중"). ② 보드 일정 카드 **왼쪽에 미니 달력**(`BoardCalendar`) — 마일스톤이 있는 달을 그려 마일스톤 날짜 표시(당일 강조). 검증: frontend tsc 클린. (서브에이전트 내부 출력 스트리밍은 output 파일 tail 필요 — 후속) | **"돌아가는지 보여줘·너무 오래 걸림" + "일정 왼쪽에 달력도" 요청, "계속 해줘"** | `frontend/src/agent/useAgentRun.ts`, `frontend/src/screens/workspace/{S3View.tsx,s3.module.css,S4View.tsx,s4.module.css}` |

| Claude Code (Opus 4.8) | **추론(thinking) 타임라인 노출 — 접힘 패널**: pi TUI처럼 추론을 보고 싶다는 요청. 브리지는 이미 `thinking_delta`를 내보내나 **프런트가 안 받고 있던 것**(frontend contract에 타입 없음·useAgentRun 미처리)을 발견. frontend contract에 `thinking_delta` 추가, useAgentRun에 `ThinkingEntry`(text와 분리, settle 양쪽 처리, 모달리티 전환 시 정리), S3View에 `ThinkingStep`(`<details>` **기본 접힘·흐린 색**, 스트리밍 중 summary 커서). `.pi/settings.json`의 `defaultThinkingLevel:"medium"` 덕에 rpc에서도 thinking 방출 → 전 경로 연결. 검증: frontend tsc 클린. **메인 에이전트(Planner) 한정** — 서브에이전트 내부 추론은 격리 실행이라 부모 rpc에 안 옴(output 파일 tail 필요, 후속) | **"thinking까지 다 가져와서 TODO처럼 히든(접힘)으로" 요청** | `frontend/src/agent/{contract.ts,useAgentRun.ts}`, `frontend/src/screens/workspace/{S3View.tsx,s3.module.css}` |

| Claude Code (Opus 4.8) | **서브에이전트 내부 출력 스트리밍**: researcher/critic 내부 작업이 부모 rpc 엔 안 와 긴 대기(critic 6분) 동안 깜깜하던 문제. pi-subagents 가 onUpdate 로 안 흘리고 `.output`(JSONL)에만 turn 마다 append 함을 소스로 확인 → 그 파일을 **백엔드가 tail**. `subagent-tail.ts`: pi-subagents `output-file.ts`의 경로(`tmpdir/pi-subagents-<uid>/<encodeCwd>/<sessionId>/tasks/<agentId>.output`)·인코딩을 그대로 복제, `fs.watch`로 새 줄만 파싱(시작 offset=기존 크기라 과거 출력 무시), 텍스트 추출(assistant 본문·tool 호출명). ws-handler 가 watch→소켓으로 `subagent_delta`(agentId,delta) 전송. 프런트: contract/useAgentRun 에 `SubagentEntry`(agentId별 누적), S3View `SubagentStep`(기본 접힘 패널). 검증: 실제 .output 에 대해 경로 일치·JSONL 파싱·텍스트 추출 확인, 백/프 tsc 클린, 백엔드 부팅 정상. (경로는 pi-subagents 내부 구조 복제라 버전 바뀌면 깨질 수 있음 — 실패해도 무해) | **"내부 출력까지 가자. 흘려보내줘" 요청** | `backend/src/{subagent-tail.ts(신규),ws-handler.ts,contract.ts}`, `frontend/src/agent/{contract.ts,useAgentRun.ts}`, `frontend/src/screens/workspace/{S3View.tsx,s3.module.css}` |

| Claude Code (Opus 4.8) | **토큰·시간 절감 1차 — 에이전트 경량화**: 실런 수치(critic 383초/67.9k·writer 61초/36.9k)로 비용 드라이버 진단. ① **monitor 경량화**(sonnet+thinking:high → **haiku-4.5+thinking:low**) — 재기획 필요성 '판정만' 하는 경량 역할인데 과배치돼 있었음. 온디맨드 트리거라 기획 루프엔 안 들어가고 자기참조 루프도 없음(안전). ② **secretary** thinking:low 명시(추출 작업, 기본 medium 추론 낭비 제거; 모델은 기본 gemini-flash 유지). ③ **writer** thinking:high → medium. **inherit_context 끄기(최대 절감)는 보류** — 검증 결과 writer는 AGENTS.md가 자료를 프롬프트로 넘기라 명시(중복 맞음)지만 **critic은 입력 계약이 없어 inherit로 초안을 봄 → 그냥 끄면 깨짐**(사용자 의심이 맞았음). critic inherit 끄려면 AGENTS.md에 critic 입력 계약 추가 선행 필요(후속). | **"monitor·secretary는 루프 밖·경량이어야"·"writer thinking만 낮춰"·"'중복' 확실하냐" 검증 요구 → critic 과장 정정** | `.pi/agents/{monitor.md,secretary.md,writer.md}` |

| Claude Code (Opus 4.8) | **"런 완료" 조기 종료 진단 + 예고-후-정지 방지 지시**: 기획이 인트로 직후 "런 완료"로 끊김. pi 소스로 `agent_end`(→done) 의미 확인 — **모델이 도구 호출 없는 최종 텍스트를 내면 루프 종료→agent_end**. Planner(gemini-2.5-flash)가 "다음에 공휴일 확인하겠습니다"를 **텍스트로 예고만 하고 calendar_list-events를 안 불러** 턴 종료된 것(브리지는 정상). flash 계열의 예고-후-정지 습성. → AGENTS.md에 ⭐**"예고만 하지 말고 같은 흐름에서 바로 도구 호출로 이어가라, 턴 종료는 ask_user_question·제안서 완성·치명 문제일 때만"** 최상위 지시 추가. (적용은 토큰 0, 검증은 다음 실행 때) | **실런 중 "계속 런완료 떠" 제보 → 먼저 "세션 종료해 토큰 막아줘"(pi·백엔드 kill) 후 원인 규명·지시 추가 승인** | `AGENTS.md` |

| Claude Code (Opus 4.8) | **Planner 모델 교체(gemini-flash→haiku) — "런 완료" 조기종료 실제 해결**: AGENTS.md 지시 추가에도 또 멈춤(estimate_budget 직후 "보드 반영하겠습니다" 말만 하고 update_state 안 부름→agent_end) → 프롬프트가 아니라 **모델 문제** 확정. Planner가 `defaultModel: gemini-2.5-flash`였는데 flash 계열이 도구 결과 후 다음 도구를 안 부르고 텍스트로 턴 종료하는 습성. settings.json `defaultModel → anthropic/claude-haiku-4.5`(체이닝 신뢰성↑). 비용 균형: **researcher·secretary는 `model: google/gemini-2.5-flash` 핀**(단순 작업·저렴, 기존에 잘 됨). 결과 라인업: Planner/writer/monitor=haiku, critic=sonnet, researcher/secretary=gemini. 다음 pi 스폰부터 적용(새로고침/새 행사). haiku도 멈추면 sonnet 승격. | **실런 재현으로 지시만으론 안 됨 확인 → 모델 교체 결정, 비용 위해 단순 서브는 gemini 유지** | `.pi/settings.json`, `.pi/agents/{researcher.md,secretary.md}` |

| Claude Code (Opus 4.8) | **타임라인 마크다운 렌더링**: 에이전트 출력이 `**굵게**`·`-` 목록 등 raw 마크다운으로 보이던 것 → `react-markdown`+`remark-gfm`(표·취소선) 도입, 공용 `Md` 컴포넌트(`components/Md.tsx`+css, 타임라인용 컴팩트 스타일). S3View의 텍스트·추론·서브에이전트 패널·도구 결과에 적용(블록 요소라 `<p>`→`<div>` 교체). 검증: frontend tsc 클린. (DocView 제안서 렌더는 후속) | **"결과 md로 렌더링도 간단하게 적용" 요청** | `frontend/src/components/{Md.tsx,Md.module.css}`(신규), `frontend/src/screens/workspace/S3View.tsx`, `frontend/package.json` |

| Claude Code (Opus 4.8) | **실런 피드백 티어1 — UI 한줄헤더·요소태깅·보드 mock 제거 + 모델 조정**: 풀런 후 다발 피드백 중 빠른 것 처리. #8 워크스페이스 헤더 한 줄: 셸이 행사명+상태+모델+탭을 한 행에(`wsTopbar`), S3View/S4View 중복 헤더 제거(S3View ws prop·TopBar 삭제). #4b 요소 태깅: `classifyElement`에 calendar/maps/gmail→MCP, ask_user_question→Extension 추가(MCP×0 오분류 해소), 타임라인에 도구 **이름 볼드**+중복 코드 제거. #5b 보드 mock 제거: useBoard 읽기전용화(잠금토글·changeWeather·doReplan 삭제), '예보 변경 시뮬레이션' 버튼 제거, 조건·예산 읽기전용, 재기획 배너 '다시 기획'→AI 에이전트로 이동. 모델: Planner→sonnet, critic→haiku. 검증: 백/프 tsc·테스트11 통과. **남은 한계**: Skill 카운트는 스킬이 도구호출이 아니라 이벤트로 못 셈(×0 유지, #7 감사에서 재설계). | **실런 피드백 다발 → 티어1(빠른 것)부터 "해줘"** | `frontend/src/routes/{WorkspaceShell.tsx,Workspace.tsx,Page.module.css}`, `frontend/src/screens/workspace/{S3View.tsx,S4View.tsx}`, `frontend/src/data/boardState.ts`, `backend/src/bridge.ts`, `.pi/settings.json`, `.pi/agents/critic.md` |

| Claude Code (Opus 4.8) | **문서=실제 제안서 렌더(#1·#2) + 작업공간 사람 편집 persist(#6, #5b 정정)**: ① 제안서가 cwd=루트에 떨어지던 문제 → 확장에 `save_report` 도구(세션ID로 `workspace/<wsId>/proposal.md` 저장, state.json과 동일 라우팅), AGENTS.md가 `write` 대신 이걸 쓰게. ② `GET /api/workspaces/:id/proposal` + DocView 재작성(mock PROPOSAL_BLOCKS 제거 → 실제 md를 `Md`로 풀폭 렌더, 셸이 헤더 담당하니 자체 헤더 제거, doc.module `.doc` max-width 해제). ③ **#5b 정정**: 사용자가 "잠금은 사람 권한·작업공간은 사람이 편집 가능해야"라 지적 → 읽기전용화 되돌리고 잠금 토글 복원하되 **mock이 아니라 백엔드 저장**: `PATCH /api/workspaces/:id`(state.json 병합, 사람은 잠금 권한자) + useBoard `persist()`로 낙관적+PATCH, CORS preflight(OPTIONS) 처리. ('예보 변경 시뮬레이션'·doReplan 데모는 계속 제거 — 재기획은 에이전트). 검증: 백/프 tsc, save_report→파일·GET·PATCH 라이브 확인 | **"문서가 mock·헤더 artefact·좌우 꽉 차게" + "잠금은 사람이·작업공간 사람 편집 가능해야" 정정** | `.pi/extensions/event-tools.ts`, `AGENTS.md`, `backend/src/{server.ts,workspace.ts}`, `frontend/src/screens/doc/{DocView.tsx,doc.module.css}`, `frontend/src/data/boardState.ts`, `frontend/src/screens/workspace/S4View.tsx` |

| Claude Code (Opus 4.8) | **작업공간 전체 사람 편집 + 산출물 정리**: "모든 항목 사람이 변경 가능해야"(stage·예산숫자·업체 포함) 요청 반영. useBoard에 편집 함수 추가(모두 PATCH로 저장): `setStage`(기획↔진행중↔정산완료 스텝 클릭), `editConditionValue`(조건 값 입력), `editBudget`(계획액 인라인 숫자), `cycleVendorStage`(업체 단계 칩 클릭 순환), 잠금 토글은 기존. S4View 위젯화(stepper/조건/예산행/업체행 버튼·입력 + CSS) + 루트 산출물(이전 음악페스티벌 proposal.md·대관신청·행동가이드)을 `workspace/w-mqoq7akl/`로 이동(루트 정리). 산출물 카드는 가짜 항목 제거하고 실제 제안서(문서) 링크만. 검증: 프런트 tsc, PATCH 라이브. **남은 것(답변)**: 날씨는 에이전트 미호출(백엔드 엔드포인트만)·문서 편집/버전 기능 복원·stage 라이프사이클(리포트)·메일 흐름·전수감사 = 후속 | **"stage도 사람이·모든 항목 편집 가능·산출물 mock·문서 2단헤더 기능 복원·날씨/캘린더/산출물 왜 안되나" 다발 피드백** | `frontend/src/data/boardState.ts`, `frontend/src/screens/workspace/{S4View.tsx,s4.module.css}` |

| Claude Code (Opus 4.8) | **놓친 피드백 정리 + ④문서편집·①날씨연결 + UI 폴리시(impeccable)**: "수정 요청 많이 반영 안 됨, 세션 뒤져 다 찾아라" → 세션 전수 점검해 8개 미완 항목 `TODO.md` 백로그화. ④ **문서 편집 복원**: DocView에 편집 토글+textarea, `PUT /api/workspaces/:id/proposal`로 저장(CORS에 PUT). ① **날씨 연결**: `get_weather`를 **pi 도구로** 추가(extension이 `backend/src/weather.ts` 재사용 — 중복 없음, jiti 로드+동작 검증), AGENTS.md에 "옥외 행사: maps MCP로 좌표→get_weather→update_state weather" 지시, bridge classify에 Extension 등록. (앞서 impeccable로 UI 폴리시: 헤더 세그먼트 토글·편집필드 hover암시·숫자 스피너 제거.) 검증: 백/프 tsc·테스트11·PUT/get_weather 라이브. **남은 백로그**: ② 카운트 재설계·③ critic 요약본·⑤ 리포트 라이프사이클·⑥ 메일흐름·⑦ 재기획 자동갱신·⑧ 전수감사 | **"반영 안 한 거 세션 뒤져 다 찾아 + 해줘"** | `TODO.md`, `.pi/extensions/event-tools.ts`, `AGENTS.md`, `backend/src/{server.ts,bridge.ts}`, `frontend/src/screens/doc/{DocView.tsx,doc.module.css}` |

| Claude Code (Opus 4.8) | **버전 이력 복원 + 날씨 기획흐름 명시**: ④ 버전 이력 — `save_report`(AI)·`PUT proposal`(사람) 시 `state.json.versions[]`에 {v,author,at,summary} 적립, GET proposal이 versions 동봉, DocView에 버전 이력 토글 패널(AI=teal·사람=amber, DESIGN §9.15). ① 날씨 — AGENTS.md를 "행사일 확정 즉시 get_weather 1회 → update_state weather" 명시 단계로 강화. 검증: 백/프 tsc, PUT 2회→versions 2건 라이브. **C(메일/secretary 분류 + 작업공간 통신 창)는 설계 확인 후 진행** | **"편집 말고 버전관리도 있었다 + 날짜 정하면 날씨 저장 + secretary 메일 분류 보는 창"** | `.pi/extensions/event-tools.ts`, `AGENTS.md`, `backend/src/server.ts`, `frontend/src/screens/doc/{DocView.tsx,doc.module.css}` |

| Claude Code (Opus 4.8) | **C: 메일/secretary 분류 + 작업공간 통신 창**: 사용자가 위치=작업공간 카드·트리거=수동 버튼 선택. `save_comms` 도구 추가(secretary 분류 결과 [{from,subject,date,relevant,reason,insight}]를 `workspace/<id>/comms.json`에 저장, 세션ID 라우팅) + `GET /api/workspaces/:id/comms` + bridge Extension 분류. AGENTS.md: "사용자가 '통신 점검' 요청 시 secretary 1회 spawn→Gmail로 이 행사 관련 메일 분류→save_comms→보드반영 제안". S4View에 통신·회의록 카드(관련/무관 마커·인사이트, DESIGN §9.28) + '통신 점검' 버튼(start 프롬프트→AI탭 이동). 검증: 백/프 tsc, save_comms 등록·저장·GET 라이브. **남음**: 자동 트리거 인프라(통신 도착시 자동) | **(질문 답) 작업공간 카드 + 수동 '통신 점검' 버튼** | `.pi/extensions/event-tools.ts`, `AGENTS.md`, `backend/src/{server.ts,workspace.ts,bridge.ts}`, `frontend/src/screens/workspace/{S4View.tsx,s4.module.css}` |

| Claude Code (Opus 4.8) | **통신 카드 이동 + 통합 '점검(운영 새로고침)' 구조**: 사용자 제안 — 매번 날씨 호출/개별 버튼 대신 한 번의 점검 파이프라인으로 재구조화. 통신 카드를 colSide(날씨↓·산출물↑)로 옮기고 표시 전용화(버튼 제거). 상태 스트립에 **'점검' 버튼** 하나 → start로 운영 점검 위임: ①secretary 새 메일 분류(save_comms) ②get_weather 행사일 날씨(변동 시 update_state) ③변경이 기획에 영향 시 ask_user_question 재기획 제안(아니면 유지). 날씨는 점검 때만 호출(매 로드 X). AGENTS.md "작업공간 점검(운영 새로고침)" 3단계로 명문화. 검증: 프런트 tsc | **"통신 카드 날씨↓산출물↑·폭축소 + 구조 변경: 리프레시→secretary→monitor→날씨·메일 변경 확인→재기획 결정"** | `frontend/src/screens/workspace/{S4View.tsx,s4.module.css}`, `AGENTS.md` |

| Claude Code (Opus 4.8) | **점검을 백그라운드로 + 재기획은 배너로 결정**: '점검'이 즉시 AI 탭으로 넘어가던 것 수정 → **작업공간에 머물며 백그라운드 실행**(런은 셸 소유). useBoard에 `reload()` 추가, S4View가 런 running true→false 전이를 감지해 보드·통신 재로드. monitor 판단 결과는 chat 즉답(ask_user_question)이 아니라 **`update_state` replanChangedInputs로 재기획 배너만** 올리고(예: "예산 9000만→9500만"), 실제 재기획은 사용자가 배너의 "AI 에이전트에서 재기획" 클릭 시. 버튼 '점검 중…' 스핀(reduced-motion 존중). AGENTS.md 점검 3단계 정정. 검증: 프런트 tsc | **"점검 누르면 바로 AI로 넘어감 — 원치 않음. 백그라운드로 날씨·메일·monitor 돌리고 재기획 배너 띄울지 monitor가 결정"** | `frontend/src/data/boardState.ts`, `frontend/src/screens/workspace/{S4View.tsx,s4.module.css}`, `AGENTS.md` |

| Claude Code (Opus 4.8) | **다발 정비**: ①날씨 basis 계산식 제거→"과거 N년 평년값 (예측치)"·예보="날짜 기상예보"(weather.ts). ②이메일 제목 통일 — vendor rfq/confirm을 `[{{행사명}}]` 시작으로(notice-writer assets). ③통신 카드 `<ul>` 기본 인덴트 제거(list-style/padding/margin 0). ④작업공간 풀폭 레이아웃 — `.col` max-width 해제, `.twoCol` `minmax(0,1fr) 360px`(좌측 채움·우측 고정), **일정을 colMain(업체 밑)으로 이동**(달력 + 축소 D-레일 + 리스트 한 카드에). ⑤stage 변경 시 헤더 상태 동기화 — store에 `setStatus` 추가, 스테퍼 클릭이 stage+status 동시 갱신. ⑥문서 디자인 — `.doc`에 명조 본문·헤딩 위계·표·여백(Md 위에 덧입힘, 에이전트 채팅 불변), 읽기 컬럼 800px. 검증: 백/프 tsc·테스트11, 날씨 재호출로 basis 갱신 | **"날씨 문구 애매·이메일[행사명]제목·통신 인덴트·작업공간 풀폭+일정 업체밑(달력 빼고)·문서 디자인·stage 바꿔도 헤더 안 변함"** | `backend/src/weather.ts`, `.pi/skills/notice-writer/assets/email-vendor-{rfq,confirm}.md`, `frontend/src/{store/workspaces.tsx,screens/workspace/S4View.tsx,screens/workspace/s4.module.css,screens/doc/doc.module.css}` |

| Claude Code (Opus 4.8) | **③ critic 입력계약 — inherit off + 요약본 전달(토큰 절감)**: critic이 부모 대화를 통째 상속(이전 측정 67.9k)하던 것 → `critic.md inherit_context:false` + 입력을 프롬프트로(ⓐ초안 전문 ⓑ제약 조건 요약 ⓒ근거 자료 요약). AGENTS.md: critic을 inherit-off 목록에 추가, writer⇄critic 루프에 critic 입력계약 명시("대화 복붙 말고 검토에 필요한 사실만 요약"). **무한루프 방지 보강**(사용자 지적): 입력계약에 ⓓ확정된 결정+근거·ⓔ이전 라운드 지적을 추가하고 critic.md에 "확정 결정 재론 금지·이미 종결 항목 안 들춤" 명시 — 원래 full inherit를 준 이유(결정 근거 모르고 루프)를 요약으로 대체. (백스톱: writer 루프 2회 캡은 기존 유지.) | **"다음거 가자"(③) + "critic 전체 준 건 무한루프 방지 때문 — 해결됐나"** | `.pi/agents/critic.md`, `AGENTS.md` |

| Claude Code (Opus 4.8) | **⑤ 완료→사례 적립(RAG 루프) + ⑥⑦ 정리 + 일정 확인**: `rag_index` API를 pi-local-rag 소스에서 확인(`{path}`). `save_case` 도구 추가(완료 행사를 cwd/cases/<slug>.md에 — save_report/comms 패턴). AGENTS.md '행사 완료 — 사례 적립' 절차(회고 ask→save_case→rag_index). S4View: '정산·완료' 클릭 시 window.confirm → setStatus(done) + 완료 프롬프트 트리거→AI 탭(회고 답변용). bridge classify에 save_case 추가. **⑥ 자동/⑦ 완전자동**은 잠긴 결정 "자동 트리거 안 함(온디맨드)"이라 점검 버튼으로 충족·보류로 정리. **일정**은 이미 실데이터(build_checklist Extension→update_state milestones, mock 아님). 검증: 백/프 tsc·테스트11, save_case 등록·저장 라이브 | **"⑤⑥⑦+실제 일정 다 해줘"** | `.pi/extensions/event-tools.ts`, `backend/src/bridge.ts`, `AGENTS.md`, `frontend/src/screens/workspace/S4View.tsx`, `TODO.md` |

| Claude Code (Opus 4.8) | **⑦ 편집 즉시 재기획 배너(가벼운 버전)**: "왜 ⑥⑦ 안 했냐" 지적 — 잠긴 결정만 핑계로 가벼운 버전을 안 만든 판단 미스 인정. useBoard editConditionValue/editBudget/editBudgetTotal이 변경분을 `replan.changedInputs`에 누적(예: "총예산 800만 → 950만")→배너 자동 갱신, dismissReplan은 백엔드 반영(reload해도 안 돌아옴). 세션 내·사용자 액션 기반이라 백그라운드 인프라 불필요(잠긴 "자동 트리거 안 함"과 무충돌). ⑥ 자동(메일 도착시 자동 secretary)은 백그라운드 Gmail 폴러=그 결정이 피한 인프라라 사용자 확인 후로. 검증: 프런트 tsc | **"6과 7은 왜 안 해줬냐"** | `frontend/src/data/boardState.ts` |

| Claude Code (Opus 4.8) | **⑥ 주기 자동 점검(페이지 열 때 staleness)**: 사용자 선택 — 풀 백그라운드 폴러 대신 "운영 단계 워크스페이스를 열었는데 마지막 점검이 1주 넘었으면 자동 1회 점검". useBoard `markChecked`(checkedAt ISO를 state.json에 PATCH) + BoardState.checkedAt, refresh()가 markChecked 호출, S4View 마운트 효과가 `stage!=="기획" && (checkedAt 없거나 >7일)`이면 refresh() 1회(ref 가드). 백그라운드 Gmail 폴러·세션-외 트리거는 여전히 안 함(잠긴 온디맨드 유지). 검증: 프런트 tsc | **"온디맨드 + 일주일 주기 폴링(페이지 열 때 마지막 업데이트 1주 전이면)"** | `frontend/src/data/boardState.ts`, `frontend/src/screens/workspace/S4View.tsx`, `TODO.md` |

| Claude Code (Opus 4.8) | **보드 일정 ↔ 제안서 액션플랜 불일치 수정**: 사용자 지적 — 보드 일정이 제안서의 실제 계획과 다름. 확인 결과 보드는 `build_checklist` **일반 템플릿 6개**(D-30/14/7/1/당일/D+7, 담당 빈칸), 제안서는 **행사별 핵심 경로 9개**(대관 D-90·보험·재해대처신고·아티스트 등, 담당 있음) — writer가 만든 실제 플랜이 `update_state(milestones)`로 안 들어감. **시스템 고침**: AGENTS.md "일정=제안서 액션플랜과 일치, build_checklist는 날짜 베이스일 뿐, 도메인 마감+owner 얹어 update_state". **데이터 패치**: 음악페스티벌 state.json milestones를 제안서 9개로 교체(PATCH). | **"이 일정이 제대로 안 들어간 것 같다, 확인해봐"** | `AGENTS.md`, (데이터) `workspace/w-mqoq7akl/state.json` |

| Claude Code (Opus 4.8) | **② 스킬 카운트 + 달력 월 이동**: ② 공식 문서(pi.dev/skills) + Pi 소스 확인 — 스킬은 task 매칭 시 에이전트가 `read`로 SKILL.md 로드(progressive disclosure). bridge `detectSkill`(read 경로 `.pi/skills/<name>/SKILL.md`→스킬명) + tool_execution_start에서 element=Skill·"스킬: <name>". cases/*.md는 제외 검증. "못 센다"던 이전 답 정정. 달력: BoardCalendar에 offset state + 이전/다음 달·'행사 달' 버튼(`.miniCalNav` 등). 검증: 백/프 tsc, detectSkill 케이스 5종 | **"스킬 카운트 + 달력 다른 month로 + 캘린더 API/MCP 안 쓴 이유?"** | `backend/src/bridge.ts`, `frontend/src/screens/workspace/{S4View.tsx,s4.module.css}` |

| Claude Code (Opus 4.8) | **일정 업데이트 — 사람 편집 + 진행상태 자동 갱신**: 사용자 선택(편집+자동, GCal 등록 제외). useBoard에 toggleMilestoneDone·editMilestone(due 바꾸면 dday 재계산)·addMilestone·removeMilestone(전부 PATCH). S4View 일정 리스트를 인라인 편집(완료 체크 토글·제목/담당/마감 입력·삭제·마일스톤 추가)으로. `effStatus`로 지난(due<오늘) 미완료는 '지연' 자동 표시(저장값 아닌 표시용), D-레일 점도 effStatus 반영. 검증: 프런트 tsc | **(질문 답) 사람 편집 + 진행상태 자동, GCal 등록 X** | `frontend/src/data/boardState.ts`, `frontend/src/screens/workspace/{S4View.tsx,s4.module.css}` |

| Claude Code (Opus 4.8) | **캘린더 등록 + 산출물 전체 목록**: ① 일정→Google Calendar 등록 버튼(calendar MCP write, 마일스톤별 종일 이벤트, 중복 skip) + AGENTS.md 절차. ② 산출물이 제안서만 보이던 것 → **워크스페이스 모든 문서(.md) 목록**: `GET /artifacts`(listArtifacts) + `GET /file?name=`(workspaceFile, 한글명·경로탈출 차단), DocView가 `?doc=<name>`이면 읽기전용 렌더(제안서만 편집·버전). 산출물 카드가 proposal·대관신청체크리스트·행동가이드 다 나열 + `save_case`가 state.json에 caseId 기록→'사례 적립됨' 링크(/case/:id). 검증: 백/프 tsc·테스트11, artifacts/file 라이브(3개 문서·한글명) | **"캘린더 등록이 낫지 않냐 + 산출물에 제안서 외 다른 문서·사례도 포함되어야"** | `backend/src/{workspace.ts,server.ts}`, `.pi/extensions/event-tools.ts`, `frontend/src/screens/doc/{DocView.tsx,doc.module.css}`, `frontend/src/screens/workspace/{S4View.tsx,s4.module.css}`, `AGENTS.md` |

| Claude Code (Opus 4.8) | **캘린더 = 실제 GCal 임베드(사용자 결정)**: 미니 달력 자리에 사용자 Google Calendar iframe(`calendar/embed?src=<VITE_GCAL_ID>` + 한국 공휴일 캘린더 overlay). '캘린더에 등록'으로 만든 이벤트가 그대로 보임. `VITE_GCAL_ID`(frontend/.env.local, `.env.*` 이미 gitignore) 미설정 시 기존 미니 달력으로 폴백(CalendarPane 분기). 검증: 프런트 tsc | **"캘린더 어떻게 하기로? → 실제 GCal 임베드"** | `frontend/src/screens/workspace/{S4View.tsx,s4.module.css}` |

| Claude Code (Opus 4.8) | **홈화면 mock 제거 + 실제 렌더**: 증상 — 홈에 '새 행사'만 뜸. 원인 ① 빈 상태 게이팅이 `inProgress(active+late)===0`이면 전체(예정·완료·달력·체크리스트)를 숨김(planning뿐인 워크스페이스는 통째로 안 보임). ② MOCK_NOTIFS·MOCK_CHECKLIST 사용. 수정: 게이팅을 `workspaces.length===0`로(있으면 표시, 진행중 없으면 '진행 예정' 자동 펼침), `useAllBoards`로 전 워크스페이스 보드 로드 → 알림(replan=재검토·1주내 마일스톤=마감임박)·체크리스트(마일스톤 미완료 우선·날짜순 상위8)를 **실데이터 파생**, MiniCalendar도 그 보드 재사용. home.ts에서 MOCK_ 배열 삭제(타입만 유지). 검증: 프런트 tsc, MOCK 잔재 0 | **"홈화면 mock 다 없애고 실제 렌더되게, 지금 새 세션 만들기밖에 안 뜸"** | `frontend/src/screens/home/{Home.tsx,home.module.css}`, `frontend/src/data/home.ts` |

| Claude Code (Opus 4.8) | **전수 감사(⑧) 픽스 — 사례 실데이터화(A2·A3)**: cases/*.md frontmatter를 백엔드가 파싱(`GET /api/cases`·`/api/cases/:id`, 손수 파서: 스칼라+budgetActual flow-list+본문) → 하드코딩 2건(CASES) 제거하고 **실제 3건**(채용·밋업·워크숍) 서빙. CaseView가 페치+본문 Md 렌더, citedBy(역추적 미추적)·"pgvector" 출처 표기 제거→"pi-local-rag · cases/<id>.md". 홈 '사례 모음' 하드코딩 링크(/case/c-meetup-2024)→실제 첫 사례·없으면 숨김. (A1 연결상태·B4 proposal.ts삭제·C5 API_BASE단일화·E 주석은 직전 커밋.) 검증: 백/프 tsc·테스트11, /api/cases 3건·연결상태 라이브 | **(사용자 전수 감사) A~E 거짓·죽은코드·중복·stale주석 정리** | `backend/src/server.ts`, `frontend/src/data/cases.ts`, `frontend/src/screens/case/CaseView.tsx`, `frontend/src/screens/home/Home.tsx` |

| Claude Code (Opus 4.8) | **RAG 검증 + 인용 드로어 + ask 모달 + 사이드바/상태바 + 프로세스 정정**: ⚠️ ⑧ 전수감사를 정적 감사로 멋대로 [x] 친 것 사용자 지적 → 되돌리고 "라이브 런" 방식 복원, 메모리 기록(사용자 소유 작업 임의 종료 금지). RAG 점검: pi-local-rag 0.4.1, `.pi/rag/rag.db`에 사례 3건 인덱싱+임베딩 완료·config 정상 → **세팅 멀쩡**(재작업 불필요). citation: Md `onCite`로 본문 `[n]` 클릭화 + DocView가 '## 출처' 파싱→우측 드로어(사례/URL 링크), AGENTS.md writer에 `[n]`+출처 섹션 지시. ask 되묻기를 타임라인 칩→중앙 모달. 사이드바 브랜드 'Pi-Event'=홈 링크 통합. 상태바 점검+칩 우측정렬. ⚠️ 통신 데이터가 `example.com` 가짜로 보임 = secretary 환각 의심(⑧에서 확인). 검증: 프런트 tsc | **"RAG 확인·citation·ask 모달·사이드바/상태바·통신 출처" + "내가 체크한 거 다시 검토"** | `frontend/src/components/Md*`, `frontend/src/screens/{doc,workspace}/*`, `frontend/src/shell/Sidebar*`, `AGENTS.md`, `TODO.md` |

| Claude Code (Opus 4.8) | **사례 목록 + 업체메일 흐름 + 감사 2차 픽스 + 전수감사 문서**: 사례 화면이 단일로 점프하던 것 → **`/cases` 목록 표**(CasesList, 행클릭→상세), 홈/CaseView 링크 정비. AGENTS.md: 제안서 저장 직후 **업체 메일 발송 `ask_user_question`**(데모 핵심 — 실행이 메일 중심). 버그: '다음 마감'이 행사일 dday로 항상 D-0 → **오늘 기준 다음 미완료 마일스톤**으로 수정. 통신 카드에 **출처 채널(메일) 태그** + save_comms channel 파라미터. **2차 감사(사용자 지적) 픽스**: 연결상태가 무조건 "연결됨"이던 것 → **"구성됨"(설정 기준·실시간 아님 명시)**, reconnect 죽은코드 제거. build_checklist 하드코딩 → **`assets/checklist.json` 외부화**(ratios.json 원칙 일관). realClient stale 주석 정리. **`docs/AUDIT_CHECKLIST.md`** 신설(⑧ 라이브 검증 항목 전수). 검증: 백/프 tsc·checklist.json 로드·/api/connections·/api/cases 라이브. **남음(확인필요)**: C 설정 divergence(settings=openrouter vs CLAUDE.md dev=Agent SDK OAuth), 연결 실제 liveness 점검, 기커밋 합치기 | **"사례목록·업체메일·다음마감·통신출처·감사문서 + 2차 감사 A/B/C + 리포 스윕 + 커밋 합치기"** | `frontend/src/screens/case/CasesList.tsx`, `App.tsx`, `frontend/src/screens/{home,workspace,aux}/*`, `backend/src/server.ts`, `.pi/extensions/event-tools.ts`+`assets/checklist.json`, `AGENTS.md`, `docs/AUDIT_CHECKLIST.md` |

| Claude Code (Opus 4.8) | **런 끊김 방지(패널 이동·재연결) + 예산 집행 입력**: ⑧ 라이브 런 중 발견 — 작업공간 패널 이동 시 런이 죽음(critic "STOPPED BY THE USER"). 원인: WS close→백엔드 `session.close()`로 pi 즉사. 3중 방어: ① 셸이 WS 유지(기존), ② realClient **자동 재연결**(백오프, 의도적 close만 제외), ③ 백엔드 **세션 레지스트리 + grace 90s + 재부착**(WS 끊겨도 pi 유지, 재연결 시 소켓만 교체·pending 보존). + 예산 '집행(spent)' 칸을 편집 입력으로(확정만 되던 것 → 집행액 입력, 변경 시 재기획 배너). 라이브 검증 — 캘린더(추석)·budget-policy 스킬·estimate_budget·researcher×3·writer 전부 실호출 확인(환각 0). 검증: 백/프 tsc·테스트11 | **"패널 가도 안 끊기게(애초에) + 예산 집행 바꾸는 법"** | `backend/src/{ws-handler.ts,pi-session.ts}`, `frontend/src/agent/realClient.ts`, `frontend/src/screens/workspace/S4View.tsx` |

| Claude Code (Opus 4.8) | **⑧ 라이브 런 디버깅 라운드(클린 런 3회로 검증)**: 자동점검이 진행 런 가로채던 가드 추가. 도구 결과가 active 전부에 붙던 버그 → **pi `toolCallId`로 정확 매칭**. 서브에이전트가 "Tool" 태그 → **"서브에이전트" 요소 신설·카운트**. **maps 연결불가 근본원인=백엔드가 .env 미로드** → `config.ts`에서 `loadEnvFile`로 키 상속(geocode 좌표 실수신 확인). 도구명은 pi 더블프리픽스 `maps_maps_*`가 맞음(내가 오판→되돌림). rag는 환각 아니라 **pi-local-rag 자동 RAG**임을 .output에서 확인. 날씨 "평년값"→"평균값". 처리 중 메시지 거부 → **streamingBehavior:"steer"**. 서브에이전트 로그 안 보임=`fs.watch` macOS append 누락 → **폴링 보강**+thinking 추출. 조사 후 턴 종료 → AGENTS 넛지. 긴 결과 펼치기·홈 달력 월이동·예산 집행 입력. **클린 런: research→게이트→writer⇄critic→steer로 save_report(제안서 11.7KB)** 완주. 개인메일 가리고 예시 워크스페이스 1개 커밋 | **"라이브 런 돌려가며 버그 즉시 수정, 환각/도구명 오판 정정"** | `backend/src/{ws-handler,pi-session,config,bridge,rpc,contract,subagent-tail,weather}.ts`, `frontend/src/{agent/*,screens/**}`, `AGENTS.md`, `workspace/w-mqp48cgt/`(예시) |

| Claude Code (Opus 4.8) | **데모 시딩 — 가짜 워크스페이스 5종 수기 작성**(녹화용, pi 미실행): 핸드오프 지침대로 `workspace/<id>/state.json`을 손으로 채워 홈/사례 화면에 "다양한 상태"가 보이게. ① **w-demo-launch**(진행중·재검토 종합 — replan 배너 + 마감임박 마일스톤 + 예산 confirmed/spent/stale 혼합 + 업체 4단계 + comms 4건), ② **w-demo-jumak**(학생회 축제 주점 — 주류·안전 조건, 예산 6항목, comms 3건, 옥외 날씨), ③~⑤ **meetup/workshop/hire**(정산·완료 — milestones 전부 done·집행완료·caseId 연결). 완료 3건은 행사명·인원·장소·예산을 **사례 파일(c-*-2024.md) 값에 맞춰 정렬**(caseId 일관성). 검증: 5×state+2×comms JSON 유효성 통과, 백엔드 `/api/workspaces` 7건 인식 확인 | **핸드오프 txt 제공 + 시딩 실행 위임; 완료 워크스페이스명을 사례 연도(2024)에 맞춘 정렬은 내가 결정·보고** | `workspace/w-demo-{launch,jumak,meetup,workshop,hire}/state.json`, `workspace/w-demo-{launch,jumak}/comms.json` (모두 gitignore·로컬 녹화자산) |

## 2026-06-26

| 도구 | AI 구현 | **내 역할 (지시·결정·피드백·검증)** | 산출물 |
|---|---|---|---|
| Claude Code (Opus 4.8) | **smoke 모드 제거 + docker 도구 로딩 결함 수정(둘 다)**: ① **SMOKE 제거** — `PI_SMOKE` 토글이 4파일(config·pi-session·ws-handler·server)에 퍼진 디버깅 군더더기 → 전부 제거(`/health`에서 smoke 필드도). ② **docker에서 커스텀 확장 미로드 근본수정**: (ㄱ) 신규 컨테이너엔 `~/.pi/agent/trust.json`이 없어 project-local 확장(`event-tools` 등) 미신뢰 → `pi-session.ts` spawn에 **`--approve`** 추가(호스트는 이미 신뢰돼 무해). (ㄴ) 빌드 때 `pi install`이 **최신** pi-web-access를 받아 pi 0.79.8 번들 pi-ai의 없는 서브패스(`/compat`) import로 로드 실패 → Dockerfile에서 **호스트 작동 버전 핀**(subagents@0.10.3·mcp-adapter@2.10.0·web-access@0.10.7·local-rag@0.4.1). **검증**: backend tsc 클린, 컨테이너 재빌드 후 `/health`={ok:true}, **컨테이너에서 `update_state(Extension)` 로드→data/workspace 쓰기 실증 + `rag_query`로 사례(c-workshop-2024) 검색 + 확장 로드 에러 0**. 호스트는 trust.json+--approve로 무영향 | **"smoke 필요?·docker 도구 안 뜸 → 둘 다 해줘"** | `backend/src/{config,pi-session,ws-handler,server}.ts`, `backend/Dockerfile`, `docs/ARCHITECTURE.md`, `.pi/extensions/event-tools.ts`(주석) |
| Claude Code (Opus 4.8) | **데이터 폴더 통합 — `cases/`+`workspace/` → `data/` 부모 하나로**: 사용자 결정(부모 폴더로 묶기, 저위험). 결합 전수 조사 후 `mv cases data/cases`·`mv workspace data/workspace`. 코드: `config.ts`에 `DATA_ROOT` 신설(REPO_ROOT는 `.pi`용 유지), `workspace.ts`·`server.ts`가 cases/workspace를 DATA_ROOT 기준으로, `event-tools.ts`의 STATE/REPORT/COMMS/CASE 경로 4개를 `cwd/data/...`로. **RAG 인덱스 무재인덱싱 재지정**: `.pi/rag/config.json` trackedPaths + rag.db `files.path`/`chunks.file_path` 절대경로를 `data/cases`로 치환(5 files·7 chunks). docker(compose 볼륨 `./data`, Dockerfile `COPY data`)·gitignore(`data/workspace/*`·`!w-mqp48cgt`)·정본 docs(ARCHITECTURE·AUDIT·PI_ELEMENTS·AGENTS·README·CLAUDE) sed 갱신(`/api/cases` 무손상 확인). **검증**: backend·frontend tsc 클린, REST `/api/cases`(data/cases 5건)·`/api/workspaces`(data/workspace 8건), **호스트 WS 라운드트립으로 `update_state(Extension)`→`data/workspace/<id>/state.json` 쓰기 실증**, gitignore 새 구조 정상. **부수 발견(별개)**: docker 컨테이너는 trust 부재+npm-global pi의 pi-web-access 모듈해석 오류로 커스텀 확장 미로드 — 폴더이동과 무관한 기존 docker 결함, 후속 | **"부모 폴더로 묶기" 결정, 합치는 의미 명확화** | `backend/src/{config,server,workspace}.ts`, `.pi/extensions/event-tools.ts`, `docker-compose.yml`, `backend/Dockerfile`, `.gitignore`, `.pi/rag/{config.json,rag.db}`, `frontend/src/screens/{case/*,home/Home}.tsx`, `docs/*`, `cases→data/cases`·`workspace→data/workspace` |
| Claude Code (Opus 4.8) | **`docker compose up` 한 방 실행 구성**: backend/frontend Dockerfile + compose + .dockerignore + .env.example. backend 이미지는 레포 루트를 `/app`에 굽고 `pi` 전역설치, **확장 4개를 컨테이너 안에서 `pi install`**(pi-local-rag better-sqlite3 네이티브를 linux prebuild로 복원 — 호스트 darwin 바이너리 복사 회피). 키는 `.env`→`env_file`로 주입(pi가 `OPENROUTER_API_KEY` env만으로 인증됨을 격리 HOME 테스트로 선검증). frontend는 vite build→nginx(SPA 폴백). 데이터(`workspace`·`cases`·`.pi/rag`)와 호스트 Google OAuth(`~/.gmail-mcp`·`~/.config/google-calendar-mcp`·`~/.pi/agent/mcp-oauth`)는 볼륨. **컨테이너 바인딩 버그 발견·수정**: 서버가 `127.0.0.1` 하드코딩 → 포워딩 미수신 → `HOST` env 도입(로컬 기본 127.0.0.1 유지, 컨테이너 0.0.0.0). 검증: 빌드 통과, `/health`·`/api/{connections,workspaces,cases}` 정상, **WS 라운드트립으로 컨테이너 내 pi 스폰→OpenRouter 응답("서울") 완주** | **5번째 npm 패키지(rpiv) 정본 여부 추궁→사장된 잔여물 규명, 실행 모드(데모 굽기) + MCP(호스트 인증 마운트) 결정, 환경변수 인증 가능성 선검증 지시** | `backend/Dockerfile`, `frontend/{Dockerfile,nginx.conf,.dockerignore}`, `docker-compose.yml`, `.dockerignore`, `.env.example`, `backend/src/{config,server}.ts` |

<!-- 새 항목: 날짜 섹션 + 표 행. AI 구현은 사람이 검토하고, 결정·피드백을 '내 역할'에 구체적으로 남긴다. -->

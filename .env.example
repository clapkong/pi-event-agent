# 레포 루트 .env 예시 — 이 파일을 .env 로 복사해서 값을 채운다.  cp .env.example .env

# OpenRouter (필수) — 에이전트/백엔드 모델 호출. 없으면 에이전트가 동작하지 않음.
OPENROUTER_API_KEY=sk-or-v1-여기에_키

# Google Maps (선택) — 지도 도구. 없으면 지도만 비활성, 앱은 정상 동작.
GOOGLE_MAPS_API_KEY=
